pipeline {
    agent any
    stages {
        stage('Print Tag') {
            steps {
                script {
                    //echo "Tag version: ${params.TAG_VERSION}"
echo "new tag is "
                }
            }
        }
        // Add more stages as needed
    }
}

